section
    base-card
        h2 {{ fullName }}
        h3 ${{ rate }}/hour
section
    base-card
        header
            h2 Intrested? Reach out now!
            base-button(link :to='contactLink') Contact
        router-view
section
    base-card
        base-badge(v-for='area in areas' :key='area' :type='area' :title='area')
        p {{ description }}